import Navigation from "./Navigation";

const Header = () => {
    return (
        <header className=" max-w-7xl mx-auto w-full">
            <Navigation />
        </header>
    );
};

export default Header;
